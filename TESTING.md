# 验证记录

## 0.5.0：简洁摘要、零基消息 ID 和完整待整理计数

- 摘要改为自然语言历史摘要与活动概览，可选少量关键记忆。来源省略时绑定本批实际消息，更新沿用 ID；无需新建事件/人物/状态/任务四表。旧结构继续读取。已解决条目退出常驻区，相关历史召回同条附后续结果，预算不足跳过该历史条目。
- 编号根因：内部索引本来为零基，但采集标题和来源输出加了 1。新输入、预览、批次标题和新来源使用酒馆消息 ID，旧结构字段在展示时转换，旧摘要内自由文字不自动改写。
- 待整理根因：原 pendingWork 在计数前跳过受保护回合，且分批可能跨空缺；开场白也被误计为用户回合。现在分开总待整理、可整理、受保护、后续排队及开场白。按时间顺序整理，不跨越未处理的受保护回合；范围显示实际消息区间。
- 55/55 测试及静态检查通过。新增覆盖 0 楼开场白加 18 轮的计数、受保护回合计入总量与顺序排队、简洁输出可空/可省略条目、0 楼来源、任务解决与历史召回、来源编辑撤销、导入导出和连续模拟批次 ID 传递。
- 本机酒馆样式中用合成记录验证悬浮统计和记忆档案，1280/375/320 像素无横向溢出，按钮横排；检查实际截图和来源 #0。没有读取或修改用户实际聊天，也未调用真实摘要模型。截图只能确认用户看到的最后消息 ID 与 MVU 警告，实际 token 窗口仍由其预设及聊天内容决定。


## 0.4.0：MVU 逐楼时间地点

- 核对本机酒馆助手 src/function/variables.ts：message 变量来自当前楼层 variables[swipe_id]。轻忆只读其中 stat_data 的显式选定字段，不解析补丁、不修改变量、不回退最新聊天变量。
- 50/50 测试通过，静态检查通过。新增覆盖选中 swipe、缺失快照、禁止对象/非法路径、未选字段不采集、状态稳定等待和聊天隔离、空正文状态消息在整理前不得压缩、批次 token 包含状态以及字段修改后撤销覆盖。
- 本机酒馆内用合成数据和模拟设置保存检查：1280、375、320 像素设置区无横向溢出，按钮不竖排；逐楼预览正常。实际截图已检查。测试容器曾因人为降低层级被宿主遮挡，恢复隔离容器层级后预览点击通过，未修改产品全局样式。
- 设置持久化调用经模拟宿主验证；本次未操作用户真实 MVU 状态、未运行真实模型，也未完成真实角色卡连续补丁、异步保存和刷新落盘的端到端验收。1.5 秒稳定观察不能证明所有外部异步任务结束，迟到变化另由来源指纹检查处理。
- 功能默认关闭，字段路径当前全局生效；旧历史缺少逐楼快照时保护原文，不自动用最新变量补齐。


## 0.3.1：补充来源校验修复

- 原因：旧规则要求状态/任务的来源是关联事件来源的子集，不能容纳后续楼层对同一约定的补充证据。改为必须与本批明确事件至少有一个共同来源，所有补充来源仍须属于本批。完全不相交时提示条目序号、事件引用及双方楼层，不自动改写来源。
- 同步调整解析、存档依赖校验和摘要提示词；强调事件内容必须支持变化，不能仅凭同场景关联。
- 47/47 测试及静态检查通过。用合成数据覆盖事件来源 [3]、更新来源 [3,5,7] 的解析、存档重校验、导出导入，以及完全不相交、越界和推测证据拒绝。未将用户私人日志写入仓库。
- 此次未重新调用真实模型；通过来源交集校验不代表证明文本之间的语义关联。默认预算未变。


## 0.3.0：人物 ID 与结构化记忆

- 2026-09-21：45/45 单元测试通过，扩展清单和全部运行时 JavaScript 静态检查通过。
- 新增九项回归覆盖：插件分配人物 ID、别名可空与同名人物不自动合并、状态和任务沿原 ID 更新、时间参照保留、错误来源与引用拒绝、推测不能直接改变状态、重复状态和已结束任务重启拒绝、原文修改后的回退、旧格式兼容及无效导入拒绝、注入区分当前状态与历史、自由文本修改停用依赖汇总，以及模拟宿主中的连续批次登记表传递与保存。
- 在本机 SillyTavern 1.19.0 中使用合成“银钥匙归还”记忆检查新版弹窗。桌面 1280、375 和 320 像素视口无横向溢出，展开批次后保存修改、查看来源按钮文字均为一行；检查了实际截图。没有修改用户聊天或业务设置，没有为此次 UI 验证调用模型。
- 已同步本机安装文件。旧存档不自动重建，新批次可保存结构化记录；自由文本编辑后的结构化恢复需显式重建。
- 本次未完成真实模型的结构化摘要质量、长期身份识别及四类向量服务验收。结构校验验证字段与依赖，不证明自然语言事实属实；不将模拟请求的成功视为真实 API 验收。


## 0.2.0：后台聊天与悬浮面板

- 新增回归覆盖：摘要期间追加聊天、等待空闲后保存、来源被改则拒绝保存、关闭后台并发后恢复取消、取消清理空闲等待，以及停止后台任务不清除已准备的前台记忆注入。
- UI 在本机酒馆样式中使用合成进度与模拟操作检查，未为界面测试发送模型请求或保存用户的业务设置。
- 36/36 单元测试及静态检查通过。桌面、375 / 320 像素视口的悬浮按钮均为单行，无面板横向溢出；检查了拖动边界、键盘移动、展开收起、暂停恢复、打开设置、隐藏恢复以及浅色表面和错误状态。隔离了测试浏览器中无关的已固定设置抽屉遮挡。
- 检查重新挂载时恢复位置与收起偏好、卸载清理与唯一面板实例。修复宿主 `touchstart/mousedown` 外部点击自动关闭设置，以及禁用后 `APP_READY` 晚到造成重新初始化的问题。
- 仍需在用户实际服务上验证并发配额、真实流式回复与群聊完整流程。单元测试中的成功保存不等于真实 API 与文件持久化的完整验收。

## 0.1.1：摘要输出截断诊断

- 用户提供的 Gemini 3.1 Pro Preview 返回标记为 `finish_reason: length`，正文 JSON 中途结束，另有独立思考内容。使用不含用户聊天文本的合成数据复现。
- 宿主适配改为保留原始响应的结束原因，再提取正文。明确拒绝输出上限截断，不采集独立思考，不修补残缺 JSON。
- 主 API 保留宿主针对模型选择的输出长度参数；新默认输出预留为 4,096，保留已有配置。有效输出最多取摘要上下文的一半，与设置校验一致。
- 新增回归检查：截断原因优先于 JSON 解析、完整代码块兼容、不同结束原因字段、截断不保存、不覆盖已有预算。
- 本次验证为合成回归测试及本地检查，未重新发送用户的聊天内容至真实模型，也未宣称真实摘要成功。

## 开发快照 0.1.0

开发基准：本机 SillyTavern 1.19.0、提交 `06bde939f`。本次上传用于保存开发进度，不作为完整验收或稳定版发布。

### 已完成的验证

2026-09-21 上传前复跑：`npm test` 26/26 通过；`npm run check` 通过。

- Node 单元测试覆盖预算与回合边界、长短混合文本、超长文本分批、来源变更、召回排序与预算、保存失败、迟到结果及临时上下文裁剪等逻辑。测试使用模拟宿主与模拟摘要，不等同于真实服务验收。
- 扩展清单、入口文件与 JavaScript 语法检查。
- 2026-09-19，在本机酒馆和 Chromium 中加载中文面板，检查初始化没有重复挂载。
- 中文按钮竖排问题已修复：宿主 `menu_button` 的 `width: min-content` 与可换行中文标签冲突。桌面、375 与 320 像素视口检查了按钮标签和按钮组换行，项目维护约定保留根因与检查要求。

### 真实 API 测试尚未通过

2026-09-19 使用当时主连接的 `glm-5.3-flash` 发送简短合成测试正文，返回 HTTP 400。宿主错误日志记录上游 `MissingSessionID`（缺少 `x-opencode-session`）。本次没有修改用户主连接配置，也没有将此测试标为成功。

需要在可用的摘要连接下继续验证成功摘要、来源校验、聊天元数据保存读回，以及实际生成提示词。独立 OpenAI 兼容连接、Text Completion 和四类向量服务尚无完整成功实测记录。

### 仍需完成

- 在真实测试聊天中验证全部参数持久化、刷新恢复、聊天切换、编辑删除、swipe、分支和群聊。
- 上百楼约定、人物别名、物品转移及伏笔兑现的摘要质量与两种召回质量。
- 真实正则作用域、顺序、深度、非幂等替换与 reasoning 组合。
- 不同主题、键盘操作和所有弹窗的完整界面验收。
- 并发修改、后台结果迟到、索引重建和所有请求失败路径的宿主级回归。

未完成上述验收前，项目保留在开发目录，不列入工作区“已完成作品”索引。
