# 验证记录

## 开发快照 0.1.0

开发基准：本机 SillyTavern 1.19.0、提交 `06bde939f`。本次上传用于保存开发进度，不作为完整验收或稳定版发布。

### 已完成的验证

2026-09-21 上传前复跑：`npm test` 26/26 通过；`npm run check` 通过。

- Node 单元测试覆盖预算与回合边界、长短混合文本、超长文本分批、来源变更、召回排序与预算、保存失败、迟到结果及临时上下文裁剪等逻辑。测试使用模拟宿主与模拟摘要，不等同于真实服务验收。
- 扩展清单、入口文件与 JavaScript 语法检查。
- 2026-09-19，在本机酒馆和 Chromium 中加载中文面板，检查初始化没有重复挂载。
- 中文按钮竖排问题已修复：宿主 `menu_button` 的 `width: min-content` 与可换行中文标签冲突。桌面、375 与 320 像素视口检查了按钮标签和按钮组换行，项目维护约定保留根因与检查要求。

### 真实 API 测试尚未通过

2026-09-19 使用当时主连接的 `glm-5.3-flash` 发送简短合成测试正文，返回 HTTP 400。宿主错误日志记录上游 `MissingSessionID`（缺少 `x-opencode-session`）。本次没有修改用户主连接配置，也没有将此测试标为成功。

需要在可用的摘要连接下继续验证成功摘要、来源校验、聊天元数据保存读回，以及实际生成提示词。独立 OpenAI 兼容连接、Text Completion 和四类向量服务尚无完整成功实测记录。

### 仍需完成

- 在真实测试聊天中验证全部参数持久化、刷新恢复、聊天切换、编辑删除、swipe、分支和群聊。
- 上百楼约定、人物别名、物品转移及伏笔兑现的摘要质量与两种召回质量。
- 真实正则作用域、顺序、深度、非幂等替换与 reasoning 组合。
- 不同主题、键盘操作和所有弹窗的完整界面验收。
- 并发修改、后台结果迟到、索引重建和所有请求失败路径的宿主级回归。

未完成上述验收前，项目保留在开发目录，不列入工作区“已完成作品”索引。
