# 轻忆扩展维护约定

先遵循上级工作区的酒馆扩展 skill。以实际安装的 SillyTavern 源码和浏览器运行结果核对集成。

## 宿主样式兼容

- 复用 `menu_button` 等宿主类时，检查其宽度、最小宽度、flex 收缩及文字换行规则，不能只检查颜色和边框。
- SillyTavern 1.19.0 的 `.menu_button` 含 `width: min-content`。中文按钮若允许断行，会缩成一个字宽；轻忆按钮必须容纳完整标签，由按钮组换行，避免按钮内逐字换行。
- 长正文需要的 `overflow-wrap: anywhere` 不应影响按钮标签。按钮的布局保护保留在 `.lightmemory .lm-button` 作用域内，不修改宿主全局样式。
- 修改面板后在真实酒馆检查桌面与 375 / 320 像素视口；展开 API、召回、备份区域，检查最长中文标签、整颗按钮换行、横向溢出及弹窗按钮。检查 computed style 和实际截图，不以语法检查或单元测试替代视觉验收。

2026-09-19 回归原因：按钮沿用了宿主的最小内容宽度，插件未覆盖宽度，又允许正文换行；首次检查尚未覆盖展开后的按钮区。修复采用 `width: auto`、`min-width: max-content`、`white-space: nowrap`，并重置按钮的断行规则。
